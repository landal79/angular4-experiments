import { BoldHoverDirective } from './bold-hover.directive';

describe('BoldHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldHoverDirective();
    expect(directive).toBeTruthy();
  });
});
